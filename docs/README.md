# Docs

`ARCHITECTURE_BOUNDARIES.md` is the exception to everything below: it is a
contract rather than a conversation, it is undated because it is meant to be
current, and parts of it are enforced by `tools/check-addon-boundaries.py`.
Read it before adding or editing anything under `addons/`.

Most the documents are actually conversations that I have had with AI platforms where I am trying to break the problem down into smaller more modular chunks of focused work units. The idea is to be no different than a whiteboarding session between a manager and an experienced developer. This is also a history of how decisions were arrived that and context needed to help build out the game in the most efficient way possible.

## Blank AI Prompt Sheet
This is used for recording AI Responses as I learn the game build process.

```
# Title

## Prompt


## Response
```